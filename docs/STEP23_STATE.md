# STEP23_STATE (super short)

- Auth: localStorage mock, ProtectedRoute, Korean error toasts.
- Closet: backend ON → /api/data/closet CRUD, OFF → localStorage only.
- Looks: still localStorage; Step 24 will sync with backend.
- Explore: public looks read + delete via backend; ownerEmail check.
